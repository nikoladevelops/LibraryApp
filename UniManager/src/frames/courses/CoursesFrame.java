package frames.courses;

import db.utility.DbInfo;
import frames.ViewFrame;

public class CoursesFrame extends ViewFrame {

    public CoursesFrame(String frameTitle, DbInfo dbInfo) {
        super(frameTitle, dbInfo);
    }

}
