package frames;

import javax.swing.*;

public class CrudFrame extends JFrame {
    public CrudFrame(String frameTitle){
        //this.setLayout(null);
        this.setTitle(frameTitle);
        this.setSize(700, 500);
        this.setDefaultCloseOperation(JFrame.HIDE_ON_CLOSE);
        //this.setLocationRelativeTo(null);
    }
}
