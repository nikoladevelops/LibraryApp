package frames.students;

import db.utility.DbInfo;
import frames.ViewFrame;

public class StudentsFrame extends ViewFrame {

    public StudentsFrame(String frameTitle, DbInfo dbInfo) {
        super(frameTitle, dbInfo);
    }

}
