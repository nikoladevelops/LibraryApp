package frames.professors;

import db.utility.DbInfo;
import frames.ViewFrame;

public class ProfessorsFrame extends ViewFrame{

    public ProfessorsFrame(String frameTitle, DbInfo dbInfo) {
        super(frameTitle, dbInfo);
    }

}
